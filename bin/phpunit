#!/usr/bin/env sh

PLUGIN_NAME=$(basename $(pwd))

set -xefu

(
	cd ../../../ || exit 1;

	pwd

	symfony php -dmemory_limit=-1 vendor/bin/phpunit --configuration="custom/plugins/${PLUGIN_NAME}/phpunit.xml" "$@"
)
